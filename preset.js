'use strict';
/* =========================================================
   APP_PRESET ＝ アプリの“顔”を1か所に集約
   ここを差し替えるだけで、食べ歩き → 御朱印 → マンホール … と
   別のコレクション帳になる（色・名前・言葉・ジャンル・地理軸・上限）。
   ※ 47都道府県のデータ／写真の圧縮設定などは「エンジン側」(app.js)。
     ここに置くのは“中身が変わるもの”だけ。
   ========================================================= */
window.APP_PRESET = {
  // 名前まわり
  name:         'コレログ',           // 画面ヘッダー / タブのタイトル（食べ歩き版）
  headerEmoji:  '🍜',                 // ヘッダー左の絵文字
  emptyEmoji:   '🍽️',                // 「まだ無い」画面の絵文字

  // 色（ライト/ダークの2組）
  brand: { light: '#c0392b', lightSoft: '#fbe9e7', dark: '#d8483a', darkSoft: '#47241f' },

  // 言葉
  labels:   { want: '行きたい', visited: '行った' }, // 状態の呼び名
  itemNoun: '店',                                     // 「店を追加」などの数え名
  namePlaceholder: '例）餃子の王将 三宮店',          // 名前欄の下書き

  // ジャンル（絞り込み・並べ替えの選択肢）
  genres: [
    'ラーメン', '寿司', '焼肉・肉', '居酒屋', 'カフェ・喫茶', '定食・食堂',
    'そば・うどん', 'イタリアン', 'フレンチ', '中華', 'カレー',
    'ファストフード', 'スイーツ', 'バー', 'その他',
  ],

  // 地理コンプ（全国制覇マップ）を使うか。集めるものが土地に紐づかないなら false
  geoEnabled: true,

  // 登録上限（写真の無料枠に収まる目安＝300）
  maxStores: 300,
};
